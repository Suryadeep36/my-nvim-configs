I have configured basic things for neovim.
It includes LSP for c, c++ and bash. this gives me features like auto completion, hints, error discovery.
I have used lazy nvim as the package manager
This config also includes the use of bufferline to see all the current active files in the buffer.
I have also configured Telescope for adding/seeing new files
besides from this i also use lazygit and yazi to manage git operations and file tree operations resp.
Will possible add more things as i get better at nvim
